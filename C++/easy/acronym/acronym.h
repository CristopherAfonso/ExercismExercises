#if !defined(ACRONYM_H)
#define ACRONYM_H

#include <string>
#include <regex>
#include <iostream>

namespace acronym {
  std::string acronym(std::string str);
}  // namespace acronym

#endif // ACRONYM_H